{
  "name": "@portfolio/frontend",
  "version": "0.1.0",
  "scripts": {
    "dev": "next dev",
    "build": "npm run generate-icons && next build",
    "export": "next export -o ../../dist/frontend",
    "lint": "eslint -c .eslintrc --ext .ts,.tsx .",
    "generate-icons": "svgr -d components/Common/Icons/ icons/"
  }
}
